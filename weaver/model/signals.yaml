groups:
  - id: span.query_song
    type: span
    stability: stable
    brief: This span represents the song being queried.
    span_kind: server
    attributes:
      - ref: media.song.name
        requirement_level: required
      - ref: media.artist.name
        requirement_level: required
      - ref: media.album.name
        requirement_level: recommended
      - ref: media.song.year
        requirement_level: recommended
      - ref: media.song.duration_ms
        requirement_level: opt_in
      - ref: media.song.genre
        requirement_level: opt_in
      - ref: user.subscription.type
        requirement_level: required
      - ref: user.id
        requirement_level: required
